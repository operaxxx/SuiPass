# SuiPass npm configuration

# Enable workspaces
workspaces=true

# Always save dependencies to package.json
save=true

# Save exact version
save-exact=true

# Use npm workspaces by default
workspace="*"

# Registry configuration (if using private registry)
# @suipass:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NPM_TOKEN}